package com.example.demo.model;

/**
 * AddressType enum represents the type of address such as home, office, permanent address, temporary address.
 */
public enum AddressType {
    HOME,
    OFFICE,
    PERMANENT_ADDRESS,
    TEMPORARY_ADDRESS
}
